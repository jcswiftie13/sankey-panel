{
  "name": "sankey-panel-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "app"
  ],
  "scripts": {
    "build": "npm run build --workspace trace-sankey && npm run build --workspace app",
    "check": "npm run build --workspace trace-sankey && node tools/golden.mjs check"
  }
}
